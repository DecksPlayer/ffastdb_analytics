name: ffastdb_analytics
description: >
  Analytical functions layer for ffastdb — groupBy, histogram, percentile,
  stddev, topN, rank, rolling average, cumulative sum, and pivot tables.
version: 0.1.2
repository: https://github.com/DecksPlayer/ffastdb_analytics

environment:
  sdk: ">=3.11.1 <4.0.0"

dependencies:
  ffastdb: ^0.2.7
dev_dependencies:
  lints: ^6.0.0
  test: ">=1.25.6 <2.0.0"
